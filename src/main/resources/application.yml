spring:
  profiles:
    active: local
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

springdoc:
  swagger-ui:
    operations-sorter: method
    tags-sorter: alpha